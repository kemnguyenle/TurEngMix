# Core — needed to load the benchmark and score predictions.
pandas>=2.0,<3
numpy>=1.24,<3
scikit-learn>=1.4,<2
scipy>=1.11        # chi-square test for the morphological-integration analysis

# Prompted LLM baselines (scripts/run_llm.py).
# The Responses/chat client and `extra_body` reasoning control need >=1.40.
openai>=1.40,<2
python-dotenv>=1.0

# Encoder fine-tuning (scripts/train_encoder.py, evaluate_encoder.py).
# transformers>=4.46 for `eval_strategy` and `processing_class`.
torch>=2.1
transformers>=4.46,<5
datasets>=2.19
accelerate>=0.30

# Tests.
pytest>=8.0
